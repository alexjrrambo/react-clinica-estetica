import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  bottom: 0;
  right: 0;
  margin: 1rem;

  img {
    height: 3rem;
    width: 3rem;
    cursor: pointer;
  }
`;
