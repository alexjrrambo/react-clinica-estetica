import styled from 'styled-components';

export const Button = styled.button`
  width: 100%;
  margin-top: 0.25rem;
  background: #7159c1;
  color: #fff;

  &:hover {
    cursor: pointer;
    opacity: 0.9;
  }
`;
